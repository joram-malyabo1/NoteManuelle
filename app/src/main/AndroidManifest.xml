<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.notemanuelle">

<uses-permission android:name="android.permission.INTERNET"/>

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".ApercuNote"></activity>
        <activity android:name=".Ordonnateur" />
        <activity android:name=".AvisOrdonnateur" />
        <activity android:name=".Taxateur" />
        <activity android:name=".Liquidation" />
        <activity android:name=".Article" />
        <activity android:name=".AssujettiPC" />
        <activity android:name=".Assujetti" />
        <activity android:name=".EntityPC" />
        <activity android:name=".Entity" />
        <activity android:name=".NumeroNote" />
        <activity android:name=".MenuPrinciale" />

        <activity
            android:name=".loginActivity"
            android:label="@string/app_name"
            android:screenOrientation="portrait"
            android:theme="@android:style/Theme.NoTitleBar.Fullscreen">
            <intent-filter>
                <action android:name="android.intent.action.VIEW" />
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>